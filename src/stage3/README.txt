Variable name encoding:
 [action id][info set]
 
 action id: F, C, or R
 
 info set: for each bcCount <= currentBCCount, [round history]
 
 round history: 
 
 
 
 
 every terminal node should have a string representing the actions of each
 player.  each one of those strings should represent the set of choices
 both players have made thus far, and their own results of chance events.